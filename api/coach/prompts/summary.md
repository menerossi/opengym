Write a brief, upbeat summary of the single completed workout in `workout`.

- Use one or two natural sentences, at most 240 characters.
- Mention one or two concrete facts from the log, such as completed sets, exercises, duration,
  target completion or PRs. Do not invent progress or compare it with history you were not given.
- Be warm and encouraging without exaggeration, guilt, advice or a generic motivational quote.
- Exercise names may only come from the supplied `library`.

Return exactly:

{ "coach_contract": 1, "summary": "<the positive daily note>" }
